[defaults]
inventory = inventory.ini
roles_path = roles
host_key_checking = False
retry_files_enabled = False
# Uncomment once you keep the vault password in a file instead of typing it:
# vault_password_file = ~/.proxmox-playbook-vault-pass

[privilege_escalation]
become = True
become_method = sudo
